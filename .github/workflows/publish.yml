# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_publish --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: publish

on: [workflow_dispatch, push]

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd Push'
        run: ./build.cmd Push
        env:
          NugetApiKey: ${{ secrets.NUGET_API_KEY }}
      - uses: actions/upload-artifact@v3
        with:
          name: nupkg
          path: nupkg
      - uses: actions/upload-artifact@v3
        with:
          name: Tomate
          path: src/Tomate.Exe/bin/Release/net7.0/linux-x64/publish/Tomate
      - uses: actions/upload-artifact@v3
        with:
          name: Tomate.exe
          path: src/Tomate.Exe/bin/Release/net7.0-windows10.0.17763.0/win-x64/publish/Tomate.exe
